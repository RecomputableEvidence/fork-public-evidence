# CSH Receiver Runs

Receiver-run records are written here only after the corpus/configuration freeze permits execution.

Each run must satisfy `schemas/cross_system_claim_handoff_run_v0_1.schema.json`.

Raw receiver output should be preserved under a stable path and bound by SHA-256. A normalized classifier input must never replace raw output.
