# CSH-S001-v0.1

Cross-System Claim Handoff Successor 001 is a separately identified successor to the blocked `cross_system_claim_handoff_v0_1` experiment.

It exists to resolve two matters before collecting a new comparative result:

1. receiver-platform identity changed after GitHub Models retirement;
2. the measurement chain from raw output → normalization → deterministic rule findings requires explicit interpretation boundaries.

It is **not** a continuation or completion of the frozen predecessor.

## Current state

```text
INTERPRETABILITY_RESOLVED
LOCAL_IMPLEMENTATION_PENDING_CI
HOSTED_ACCESS_PREFLIGHT_PENDING
NOT_FROZEN
NOT_EXECUTED
```

## Read in order

1. `PREREGISTRATION_v0_1.md`
2. `MEASUREMENT_AND_CODING_SPEC_v0_1.md`
3. `RECEIVER_REGISTRY_CANDIDATE_v0_1.json`
4. `EXECUTION_GATE_v0_1.json`

## Primary outcome

`U` = unsupported-inheritance **rule findings per classifiable hosted-receiver run**.

It is not a count of distinct acts, claims, harms, or legal violations.

Complementary coordinates preserve distinct promoted claims (`P`), affected-run indicator (`A`), and task state (`T`).

## Execution boundary

No corpus-bearing hosted execution is allowed until the execution gate is fully satisfied and a successor freeze record binds the exact packets, projection bytes, receiver registry, run order, coding method, classifier, and analysis program.

Neutral receiver access preflight is permitted because it contains no experiment corpus, treatment, or scoring material.

## Non-claims

This successor package does not prove Fork reduces unsupported inheritance, establish production readiness, authorize deployment, establish truth, certify compliance, or inherit standing from the predecessor.
