# Fork Shayne PR #64 recomputation admission candidate — 2026-07-30 v0.1

Status: `PROPOSED_APPEND_ONLY_ADMISSION`

Reviewer-declared disposition: `REPRODUCED_WITHIN_DECLARED_SCOPE`

This successor record proposes bounded admission of Shayne's attributable PR #64 recomputation transmission into `preservation/clean-continuance-v0.1`. It does not amend the July 18 Mac McFall / M87 record, rewrite PR #64, or widen PR #63's execution standing.

The machine-readable anchor is `FORK_SHAYNE_PR64_RECOMPUTATION_ADMISSION_CANDIDATE_2026_07_30_v0_1.json`.

## Exact subject

- PR #64 reviewed head: `d911ad5c33e0ec32037414effa7749326983d5ff`
- PR #64 merge commit: `528f4306acf75b9b4e349aaf191fcda2c1c1430b`
- platform scope: macOS 15.7.7
- runtime scope: Python 3.14.4
- reviewer-declared disposition: `REPRODUCED_WITHIN_DECLARED_SCOPE`

The source states the short head `d911ad5c`; this record resolves it to the full immutable PR #64 head using repository metadata.

## Exact-source boundary

The supplied transmission is preserved byte-for-byte and digest-bound. It says that a full findings memo was attached, but that attachment was not supplied to this repository construction.

Accordingly:

- the transmission summary is preserved and eligible for bounded admission;
- the referenced attachment is `REFERENCED_NOT_RECEIVED`;
- exact commands, complete outputs, the complete exit-code record, and the attachment's full limitations are not reconstructed;
- the missing bytes remain an explicit evidence gap.

Admission of this candidate would not silently convert the referenced attachment into a repository-held artifact.

## Preserved findings and observation

The transmission reports exact acquisition, hash-locked installation, `FRESH_RECOMPUTATION_PASS`, no candidate checkout in the trusted lane, a disposable runner workspace, byte-identical receipt recomputation, 8/8 IVS tests, deterministic receipt hashes across four hash seeds, independently recomputed merge base, candidate-SHA tamper rejection with exit 2, adverse-first verdict precedence, Git-object predecessor checks, and the continuing `STRUCTURALLY_READY_EXECUTION_BLOCKED` state of PR #63.

The changed-path result remains an observation:

`CHANGED_PATH_INVENTORY_TRANSITIVELY_BOUND_NOT_FIRST_CLASS_ENUMERATED`

Commit binding transitively covers the tree. First-class path enumeration could improve reviewer ergonomics, but the observation establishes no integrity gap or recomputation failure and does not retroactively change PR #64's standing.

## Preserved negative process evidence

A crude representation nearly caused a false `19 of 20 paths missing` finding. Reading the canonical binding mechanism caused the reviewer to withdraw it before transmission. The record preserves that correction sequence as a degraded-representation near miss, not as a defect.

## Admission and execution boundary

Opening this candidate, passing checks, or later merging it does not create endorsement, GitHub-native approval, merge authority, certification, execution permission, or authority transfer. PR #63 remains `STRUCTURALLY_READY_EXECUTION_BLOCKED`; Pair-001 remains unauthorized.

This candidate cannot pre-bind its own future merge. Any merge requires separate explicit authorization.
