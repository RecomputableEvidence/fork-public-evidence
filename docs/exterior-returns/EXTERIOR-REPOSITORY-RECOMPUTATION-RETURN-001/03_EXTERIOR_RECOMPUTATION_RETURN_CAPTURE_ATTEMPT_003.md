This is a substantial, mature CI setup. One thread jumps out immediately — `admissions/FORK-STACKED-CADENCE-BOUNDED-R&D-001/v0.1.5-QUALIFIED-FREEZE/` is the exact research line I independently verified last turn at v0.1.2. Let me follow that forward first, since I have direct working context there.I cloned the repository directly rather than browsing it, and ran as much of its own verification apparatus as I could reproduce locally.

**Scale:** 2,132 tracked files, 586 commits on `main`, PRs merged through #181, 89 tool scripts, 87 schemas, 8 formal admission lines, 11 GitHub Actions workflows running on both Ubuntu and Windows.

**What actually runs clean:** The full pytest suite — `python -m pytest -q` — passes **684/684**. I also ran roughly a dozen of the standalone checkers CI invokes independently (proof-surface state, JSON-schema bundle, reviewer access-path integrity, the CSH scaffold, the three Day-0 longitudinal checks, the meta-evidence package, the authority-state-invariance harness — which correctly discriminates all 8 valid and 12 invalid fixture cases by exact error code — current-state routing, standing-freshness): all clean.

**Live cross-checks against the repo's own claims:** `CURRENT_STANDING.md` cites its binding horizon as commit `115992d...` / PR #178 — I confirmed that commit is exactly the PR #178 merge, byte-accurate. The `2026-09-24 open-candidate snapshot` claims 18 open PRs with exact head SHAs; I pulled `refs/pull/N/head` live for all 18 (this bypasses GitHub's REST rate limit, which I hit on the first attempt) and every head SHA matched exactly, with all 18 confirmed not merged into `main`. I couldn't independently distinguish "open" from "closed-without-merge" for those 18, since that specific field needs the rate-limited REST API — everything I *could* check lines up with the document's claims.

**Continuity with the package I verified earlier this session:** `FORK-STACKED-CADENCE-BOUNDED-R&D-001` has moved well past v0.1.2. The repo shows it hit a genuine qualification **failure at v0.1.4** (frozen as `NEGATIVE_EMPIRICAL_RESULT_FROZEN`, not erased), then a repair at **v0.1.5** — 291/291 internal checks, 182/182 independent recomputation, 9/9 final adjudication criteria — admitted to `main` via PR #151. I re-ran that admission's verifier and independently re-hashed its `SHA256SUMS.txt` myself rather than trusting its printed summary; both are clean.

**Two real, small gaps found:**

1. The top-level `CHECKSUMS_SHA256.txt` has one mismatch — `research/standards/README.md` — dating to the exact commit that introduced both files. I confirmed it's a genuine hash error (not a line-ending/checkout artifact) and that no script or CI workflow anywhere in the repo actually reads this file, so nothing would ever catch it.
2. The v0.1.5 admission's own `README.md` still states its status as `CANDIDATE_PENDING_PR_CHECKS_AND_MAIN_MERGE`, but it merged via PR #151 well before `HEAD` — a stale self-description left over from before merge.

**Scope note:** this pass covered the entry points, the full automated test/checker surface, the live-PR cross-check, and one admission line traced in depth. It did not exhaustively re-derive every one of the other 7 admission lines, the 39 example sets, or the ~143 `docs/` subdirectories (Fork II, LS-MDRC-002, CSH-S001, the meta-evidence experiment registry, etc.) at the same depth. The evidence from what I did check is consistent with the repository's own self-description: a large, actively-worked research surface whose automated verification currently passes in full, with its standing layer's specific citations holding up under independent, live re-derivation rather than just being read and trusted. Happy to go as deep as you want on any one of those untraced threads next.