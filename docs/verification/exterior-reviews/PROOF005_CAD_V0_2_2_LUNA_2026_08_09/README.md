# PROOF-005 CAD v0.2.2 — Luna exterior review preservation

Status: `PRESERVED_REVIEW_RETURN_INCONCLUSIVE`

This record preserves the GPT-5.6 Luna exterior recomputation attempt supplied against frozen PROOF-005 CAD v0.2.2 exact head:

`9e3cdd2fb6d67f9abd6233ce673341bd817d6338`

The reviewer-declared disposition is preserved verbatim as:

`REVIEW_INCONCLUSIVE`

## What the review established

The review independently supported candidate identity and ancestry through GitHub, historical predecessor preservation, representative blob identity, exact checker-source inspection, and portions of the R003/R004/R005 pressure surface.

The previously preserved Gemini Unicode-escape duplicate-key hypothesis was mechanically challenged against the reproduced parser semantics and rejected. That observation is preserved as a bounded mechanism result, not as a substitute for exact-head candidate execution.

The reviewer also originated a new representational edge: a `source_refs` member consisting solely of U+200B ZERO WIDTH SPACE is accepted by the declared predicate `isinstance(ref, str) and bool(ref.strip())`. No authority, admission, proof, readiness, execution, or standing promotion was demonstrated from that accepted edge.

The U+200B result is therefore preserved as:

`OBSERVED_ACCEPTED_REPRESENTATIONAL_EDGE`

and as an inadequacy question for later governance rather than an automatic correction-required finding.

## What the review did not establish

The reviewer could not acquire a fresh local exact-head checkout because the execution environment could not resolve `github.com`. It therefore did not execute the v0.2, v0.2.1, or v0.2.2 candidate checkers or focused test suites from a detached checkout at the frozen candidate SHA.

The reviewer's repeated label `NOT_EXECUTED_DUE_TO_SESSION_LIMIT` is preserved as reviewer-authored wording. The same note expressly identifies the practical cause as the network-resolution constraint. The governance causal classification is therefore:

`ENVIRONMENT_NETWORK_RESOLUTION_CONSTRAINT`

No exact-head end-to-end exterior pass is established.

## Artifact preservation

The originally supplied markdown attachment is preserved exactly through a deterministic `gzip -n` representation.

- original filename: `Pasted markdown(20260810-024558).md`
- original size: `16908` bytes
- original SHA-256: `8accaad75340e5af6da06dbb28ee17a945d7218ac5e8056b519775698c31466d`
- preserved gzip size: `6040` bytes
- preserved gzip SHA-256: `4d2c36a7f33c6303cbea8dc0d4692b3680842a282b60e7a4a97a5f9aec1778c3`
- Git blob: `c2bd65d72bc44ec3e6a42fe1f53a277a0fa90673`

Decompressing the `.md.gz` artifact reconstructs the exact uploaded markdown bytes.

## Governance interpretation

This preservation does not establish v0.2.2 conformance or non-conformance and does not satisfy the executable exterior-recomputation gate.

It does not authorize a v0.2.3 successor solely from the U+200B observation.

PR #128 remains frozen and unmerged at `9e3cdd2fb6d67f9abd6233ce673341bd817d6338`.

The next required event remains an executable exterior recomputation against that exact coordinate, preferably using an offline Git bundle to eliminate dependency on reviewer network access.

## Non-effects

No PROOF-005 admission, proof-packaging admission, source-grounding completion, model-standing promotion, execution authority, pilot or production readiness, compliance/legal sufficiency, endorsement, or authority transfer/inheritance is created by this record.
