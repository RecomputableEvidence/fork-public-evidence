# Shayne PR #64 macOS recomputation transmission — 2026-07-30

Status: `PROPOSED_APPEND_ONLY_EXTERIOR_RECOMPUTATION_RECORD`

Reviewer-declared disposition: `REPRODUCED_WITHIN_DECLARED_SCOPE`

This package preserves the exact text supplied as Shayne's attributable transmission summary concerning Fork PR #64 at head `d911ad5c33e0ec32037414effa7749326983d5ff`.

The source bytes are preserved unchanged under `source/` and bound by `SHA256SUMS`. The machine-readable adjudication is `RECOMPUTATION_RECORD_v0_1.json`; the withdrawn heuristic near miss is separately preserved in `PROCESS_EVIDENCE_v0_1.json`.

## Source-completeness boundary

The transmission states that a full findings memo was attached. That attachment was not supplied to this repository construction. The repository therefore preserves and digests the transmission summary only. It does not reconstruct, quote, or claim possession of the referenced attachment's exact commands, outputs, exit-code record, complete environment record, or complete limitations text.

This is an evidence gap, not a reason to rewrite or discard the attributable transmission.

## Preserved disposition and observation

The reported recomputation covers a fresh macOS 15.7.7 clone with Python 3.14.4, hash-locked dependency installation, a passing disposable-workspace runner, byte-identical receipt recomputation, 8/8 IVS tests, four hash-seed conditions, merge-base recomputation, candidate-SHA tamper rejection, adverse-first verdict precedence, Git-object predecessor checks, and the continuing `STRUCTURALLY_READY_EXECUTION_BLOCKED` state of PR #63.

The changed-path observation is:

`CHANGED_PATH_INVENTORY_TRANSITIVELY_BOUND_NOT_FIRST_CLASS_ENUMERATED`

Its effects remain:

- `NO_INTEGRITY_GAP`
- `NO_RECOMPUTATION_FAILURE`
- `REVIEWER_DERIVATION_REQUIRED`

Enumerating and digest-binding the changed-path inventory remains a possible successor improvement. It does not retroactively modify PR #64's standing.

## Preserved process evidence

The reviewer nearly produced a false `19 of 20 paths missing` finding from a crude path-extraction heuristic. Inspection of the canonical commit-SHA binding mechanism caused the finding to be withdrawn before transmission. The near miss is preserved as degraded-representation process evidence, not as a PR #64 defect.

## Continuing boundary

PR #63 remains `STRUCTURALLY_READY_EXECUTION_BLOCKED`. This package grants no endorsement, merge authority, GitHub-native approval, execution authorization, Pair-001 permission, universality claim, certification, or authority transfer.
