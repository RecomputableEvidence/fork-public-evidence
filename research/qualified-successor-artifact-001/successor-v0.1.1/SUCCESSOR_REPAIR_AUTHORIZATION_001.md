# QSA-001 v0.1.1 Successor Repair Authorization 001

This authorization opens a narrow successor implementation from the frozen QSA-001 v0.1 reference. It does not modify or reinterpret the predecessor.

## Authorized

- `QSA-RC-001` — independently enforce temporal qualification at submission, inspection, and adoption.
- `QSA-RC-002` — generalize the reference chain enough to exercise and reject stale-parent adoption.
- `QSA-RC-003` — introduce explicitly segregated test-only fixture signing so deeper relational gates can be exercised with signatures remaining mathematically valid.

## Not authorized

No change to the QSA conceptual kernel, signature-validity semantics, non-inheritance rules, frozen v0.1 bytes, prior pressure returns, bootstrap standing, or production/governance claims.

## Required successor evidence

The successor must preserve the v0.1 reference unchanged, run the baseline regression, rerun A1–A12, execute focused repair probes `A4-R`, `A5-R`, `A6-R1`, `A6-R2`, `A6-R3`, and `A7-R`, and preserve any new bounded failure before any further repair.

```text
FAILURE_OBSERVED
!=
REPAIR_ALREADY_AUTHORIZED_BEYOND_THIS_SCOPE
```
