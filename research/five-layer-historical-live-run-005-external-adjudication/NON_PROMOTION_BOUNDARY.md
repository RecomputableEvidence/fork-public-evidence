# Non-promotion boundary

Preparing or merging this dispatch does not itself create an external adjudication result.

`PACKET_PREPARED != EXTERNAL_ADJUDICATION_RETURNED`

`EXTERNAL_ADJUDICATION_RETURNED != FRAMEWORK_VALIDATED`

`EXTERNAL_ADJUDICATION_RETURNED != SYNTHETIC_LANE_AUTHORIZED`

The external reviewer must commit a Phase A disposition before Phase B reference material is used for reconciliation if the two-phase separation is to be preserved.

No synthetic M21–M25 adversarial execution begins from adjacency to this dispatch. A returned adjudication record must first be preserved as a separately identified object.
